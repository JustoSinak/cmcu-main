# Application Configuration
APP_NAME=CMCU
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=http://cmcuapp.com

# Logging Configuration - Optimized for production
LOG_CHANNEL=daily
LOG_LEVEL=warning
LOG_DEPRECATIONS_CHANNEL=null
LOG_DAYS=7
LOG_STACK=single

# Database Configuration - CRITICAL: Use MySQL for production
# DB_CONNECTION=mysql
DB_CONNECTION=sqlite
DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=cmcuapp_prod
# DB_USERNAME=cmcuapp_user
# DB_PASSWORD=your_secure_password_here

# Database Performance
DB_PERSISTENT=true
DB_POOL_SIZE=10
DB_TIMEOUT=30

# Redis Configuration - Windows Optimized
REDIS_CLIENT=predis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Separate Redis databases for isolation
REDIS_DB=0              # Default
REDIS_CACHE_DB=1        # Application cache
REDIS_SESSION_DB=3      # User sessions  
REDIS_QUEUE_DB=2        # Background jobs

# Redis Connection Pool
REDIS_PERSISTENT=true
REDIS_TIMEOUT=2.0
REDIS_READ_TIMEOUT=2.0
REDIS_RETRY_INTERVAL=100

# Redis Performance
REDIS_SERIALIZER=php
REDIS_COMPRESSION=true

# Cache Configuration
CACHE_DRIVER=redis
CACHE_PREFIX=cmcuapp_cache

# Session Configuration - Optimized
SESSION_DRIVER=redis
SESSION_LIFETIME=480
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null
SESSION_SECURE_COOKIE=false
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=lax
SESSION_CONNECTION=session

# Queue Configuration
QUEUE_CONNECTION=redis
QUEUE_FAILED_DRIVER=database-uuids
QUEUE_RETRY_AFTER=90

# Broadcast Configuration
BROADCAST_DRIVER=redis

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@cmcuapp.com
MAIL_FROM_NAME="${APP_NAME}"

# Scout Search Configuration
SCOUT_DRIVER=algolia
SCOUT_QUEUE=true
SCOUT_CHUNK_SIZES=500
ALGOLIA_APP_ID=
ALGOLIA_SECRET=
ALGOLIA_APP_KEY=

# Asset Configuration
ASSET_URL=null
VITE_ASSET_URL="${ASSET_URL}"

# Performance - Laravel Octane
OCTANE_SERVER=roadrunner
OCTANE_HTTPS=false

# Image Optimization
IMAGE_DRIVER=gd
IMAGE_CACHE_ENABLED=true
IMAGE_CACHE_TIME=2628000

# View Cache Path
VIEW_COMPILED_PATH=storage/framework/views

# Filesystem
FILESYSTEM_DISK=local

# Response Cache (Spatie)
RESPONSE_CACHE_ENABLED=true
RESPONSE_CACHE_DRIVER=redis
RESPONSE_CACHE_LIFETIME=3600

# Telescope (Disable in production for performance)
TELESCOPE_ENABLED=false

# Debugbar (Disable in production)
DEBUGBAR_ENABLED=true

# License Bypass (for testing only)
BYPASS_LICENSE=false


VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"